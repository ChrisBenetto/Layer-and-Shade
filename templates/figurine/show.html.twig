{% extends 'base.html.twig' %}

{% block title %}Figurine{% endblock %}

{% block body %}
    <h1>Figurine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ figurine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ figurine.name }}</td>
            </tr>
            <tr>
                <th>Create_at</th>
                <td>{{ figurine.createAt ? figurine.createAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Upvote</th>
                <td>{{ figurine.upvote }}</td>
            </tr>
            <tr>
                <th>Downvote</th>
                <td>{{ figurine.downvote }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('figurine_index') }}">back to list</a>

    <a href="{{ path('figurine_edit', {'id': figurine.id}) }}">edit</a>

    {{ include('figurine/_delete_form.html.twig') }}
{% endblock %}
